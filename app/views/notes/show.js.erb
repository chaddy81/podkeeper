if (window.innerWidth >= 768) {
  $('.discussion-thread--desktop').html("<%= escape_javascript render 'notes/show' %>");
  $('.spinner').spinner({ min: 0, icons: { down: "icon-arrow-down", up: "icon-arrow-up" } });
}